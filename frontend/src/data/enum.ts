export enum Role {
    ADMIN = "ADMIN",
    READERS = "READERS",
    CREATORS = "CREATORS",
}

export enum Levels {
    PUBLIC,
    PROTECTED,
    PRIVATE,
}

export enum LANGUAGE {
    ES = "es",
    EN = "en",
}

export enum SEVERITY {
    WARNING = "warning",
    ERROR = "error",
    SUCCESS = "success",
}

export enum CATEGORIES {
    PDF = "PDF",
    TEXT = "TEXT",
    IMAGE = "IMAGE",
    VIDEO = "VIDEO",
    URL = "URL",

}
